function big(img) {
    document.querySelector('.big').src = img;
}
function path(color) {
    const guga= document.querySelector('.path');
    duga.style.background = color;
}
function bg(background) {
    const concolr = document.querySelector('.cont');
    concolr.style.background = background;
}